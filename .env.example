# Variáveis de ambiente (frontend)
# Copie para .env e preencha
GEMINI_API_KEY=coloque_sua_chave
# URL base da API (ex: https://seu-backend.onrender.com/api ou somente https://seu-backend.onrender.com se rotas começam com /api)
VITE_API_BASE=

# -----------------------------
# Firebase (Firestore) - Chat
# Copie os valores do painel do Firebase (Config do app web)
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# Instruções rápidas:
# 1) Acesse https://console.firebase.google.com e crie/abra seu projeto.
# 2) No menu do projeto: Configurações do projeto -> Seus apps -> Adicionar app (ícone web).</n+# 3) Registre o app e copie o objeto de configuração (apiKey, authDomain, projectId, storageBucket, messagingSenderId, appId).
# 4) Cole esses valores nas variáveis acima e salve em `.env.local`.
# 5) Reinicie o dev server: `npm run dev`.
